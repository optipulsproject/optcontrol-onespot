% This package loads packages in addition to those included in
% numapde-packages.sty. It also defines additional commands
% specific to the publication.

% Load and configure packages
\RequirePackage{tikz}
\RequirePackage{tikz-3dplot}
\RequirePackage{amsmath}

% Define additional commands
\usepackage[ruled,vlined]{algorithm2e}

